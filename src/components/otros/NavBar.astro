---
import ThemeToggle from "@/components/otros/ThemeToggle.astro";
interface Props {
  navItems: any;
  rutaLeng: string;
  ui: any;
}
const { navItems, rutaLeng, ui } = Astro.props;
let [label, label2, label3] = rutaLeng.split("/");
label3 = label3 || label2 || label;
---

<header
  class="fixed top-0 z-10 flex items-center justify-end md:justify-center w-full mx-auto mt-2"
>
  <!-- Navbar -->
  <nav
    id="navbar"
    class="hidden menu grid-hidden md:flex md:flex-wrap px-3 text-sm font-medium md:rounded-full rounded-lg text-gray-600 dark:text-gray-200 justify-center items-center"
  >
    {
      navItems.map((link: Props["navItems"]) => (
        <a
          class={`px-2 py-2 md:px-4 md:py-2  md:ml-0 transition hover:text-yellow-500 dark:hover:text-yellow-400`}
          aria-label={link.label}
          href={link.url}
        >
          {link.title}
        </a>
      ))
    }
    <a class="px-2 py-2 md:px-4 md:py-2 ml-2 md:ml-0"><ThemeToggle ui={ui} /></a
    >
    <a
      href={rutaLeng}
      class="px-2 py-2 md:px-4 md:py-2 ml-2 md:ml-0 transition hover:text-yellow-500 dark:hover:text-yellow-400"
      >{label3}</a
    >
  </nav>
  <!-- Botón para mostrar/ocultar el navbar en pantallas pequeñas -->
  <button
    id="toggleButton"
    class="block md:hidden rounded-full px-4 py-2 text-gray-600 hover:text-gray-900 transition"
  >
    <!-- Ícono de menú (puedes cambiarlo según tu preferencia) -->
    <svg
      class="w-8 h-8"
      fill="none"
      stroke="currentColor"
      viewBox="0 0 24 24"
      xmlns="http://www.w3.org/2000/svg"
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="2"
        d="M4 6h16M4 12h16m-7 6h7"></path>
    </svg>
  </button>
</header>

<script>
  const initializeNavbar = () => {
    const navbar = document.getElementById("navbar");
    const toggleButton = document.getElementById("toggleButton");

    if (!navbar || !toggleButton || navbar.dataset.navReady === "true") {
      return;
    }

    navbar.dataset.navReady = "true";

    const menuLinks = document.querySelectorAll(".menu a[href^='#']");
    const sections = Array.from(menuLinks)
      .map((menuLink) => {
        const hash = menuLink.getAttribute("href") ?? "";
        const section = hash ? document.querySelector(hash) : null;
        return {
          link: menuLink,
          section,
        };
      })
      .filter(({ section }) => section != null);

    const clearActiveLinks = () => {
      menuLinks.forEach((menuLink) => {
        menuLink.classList.remove("text-yellow-500", "dark:text-yellow-400");
      });
    };

    const updateActiveLink = () => {
      if (sections.length === 0) {
        return;
      }

      const activationLine = window.innerHeight * 0.35;
      let activeItem = sections[0];

      sections.forEach((item) => {
        if (!item.section) {
          return;
        }
        const top = item.section.getBoundingClientRect().top;
        if (top <= activationLine) {
          activeItem = item;
        }
      });

      clearActiveLinks();
      activeItem.link.classList.add("text-yellow-500", "dark:text-yellow-400");
    };

    let ticking = false;
    const onScroll = () => {
      if (ticking) {
        return;
      }
      ticking = true;
      requestAnimationFrame(() => {
        updateActiveLink();
        ticking = false;
      });
    };

    window.addEventListener("scroll", onScroll, { passive: true });
    window.addEventListener("resize", onScroll);
    updateActiveLink();

    // Función para alternar la visibilidad del navbar
    function toggleNavbar() {
      navbar?.classList.toggle("grid-hidden");
      toggleButton?.classList.toggle("open");
    }

    // Escuchar el clic en el botón de alternancia
    toggleButton?.addEventListener("click", toggleNavbar);

    menuLinks.forEach((menuLink) => {
      menuLink.addEventListener("click", () => {
        navbar.classList.add("grid-hidden");
        toggleButton.classList.remove("open");
      });
    });
  };

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", initializeNavbar, {
      once: true,
    });
  } else {
    initializeNavbar();
  }

  document.addEventListener("astro:page-load", initializeNavbar);
</script>

<style>
  nav {
    animation: nav-shadown 1s linear both;
    animation-timeline: scroll();
    animation-range: 0 100px;
  }

  button {
    animation: nav-shadown 1s linear both;
  }

  @keyframes nav-shadown {
    to {
      box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
      backdrop-filter: blur(8px);
      background-color: rgb(209 213 219 / 0.6);
      outline: 1px solid rgb(255 255 255 / 0.1);
    }
  }
  @media (prefers-color-scheme: dark) {
    @keyframes nav-shadown {
      to {
        background-color: rgb(31 41 55 / 0.6);
      }
    }
  }

  /* Estilos para pantallas pequeñas */
  @media (max-width: 767px) {
    .menu {
      display: grid;
      grid-template-columns: repeat(5, 1fr);
    }
    .grid-hidden {
      display: none;
    }
    /* Estilo adicional para el botón de alternancia abierto en pantallas pequeñas */
    #toggleButton.open svg {
      transform: rotate(90deg);
    }
  }
</style>
